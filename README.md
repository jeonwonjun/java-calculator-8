# java-calculator-precourse

## 기능 정리
1. 입력
- "덧셈할 문자열을 입력해 주세요\n"
- 문자열 입력

2. 구분자 구하기
- 커스텀표시로 구분자 구하기("//" + "\n")

3. 검증
- 잘못된 커스텀 구분자(문자, 숫자)
- 구분자 이외의 특수기호
- 구분자로 구분된 값이 숫자가 아닌 경우
- '.'이 있다면 소수점으로 계산

4. 계산
- 빈 문자열 → return 0
- 덧셈 계산

5. 출력
- 구분자(기본 구분자(',', ':') + 커스텀 구분자)로 구분된 숫자의 합
